---
import BaseLayout from "@/layouts/BaseLayout.astro";
---

<BaseLayout title="Subscribe" description="Subscribe to Mr Panda weekly updates: action list, project retrospectives, and resources.">
  <div class="page-shell grid gap-8 py-10 md:grid-cols-[1.1fr_1fr] md:py-14">
    <section class="fade-rise">
      <p class="section-kicker">Subscribe</p>
      <h1 class="page-title mt-4">What you will receive every week</h1>
      <div class="mt-5 space-y-4 text-sm leading-7 text-ink/72">
        <p>1. Three executable actions (Learn / Build / Share).</p>
        <p>2. One real project retrospective with metrics.</p>
        <p>3. One copy-ready resource (template, checklist, or tool).</p>
      </div>

      <div class="mt-8 rounded-2xl border border-line/80 bg-paper/90 p-5">
        <h2 class="font-display text-2xl text-ink">Frequency & format</h2>
        <p class="mt-3 text-sm leading-7 text-ink/70">
          One email every Monday. Core content is readable within 5 minutes, followed by clear next steps.
        </p>
        <p class="mt-3 text-sm leading-7 text-ink/70">
          Privacy: only used for site updates, never shared with third parties, unsubscribe anytime.
        </p>
      </div>

      <p class="mt-4 text-xs leading-6 text-ink/60">
        This is currently a placeholder flow and will not send real emails yet.
      </p>
    </section>

    <section class="fade-rise delay-1">
      <form action="/subscribe/success" method="get" class="space-y-5 rounded-2xl border border-line/80 bg-paper/90 p-5 shadow-card sm:p-6">
        <div>
          <label for="email" class="mb-1 block text-sm text-ink/70">Email</label>
          <input
            id="email"
            name="email"
            type="email"
            required
            placeholder="you@company.com"
            class="w-full rounded-xl border border-line bg-canvas/20 px-3 py-2 text-sm text-ink outline-none transition focus:border-accent focus:ring-2 focus:ring-accent/20"
          />
        </div>

        <div>
          <label for="role" class="mb-1 block text-sm text-ink/70">Your role</label>
          <select
            id="role"
            name="role"
            class="w-full rounded-xl border border-line bg-canvas/20 px-3 py-2 text-sm text-ink outline-none transition focus:border-accent focus:ring-2 focus:ring-accent/20"
          >
            <option value="developer">Developer / Technical</option>
            <option value="product">Product / Operations</option>
            <option value="creator">Creator</option>
          </select>
        </div>

        <p class="text-xs leading-5 text-ink/55">By submitting, you agree to receive weekly updates. You can unsubscribe anytime.</p>

        <button
          type="submit"
          class="inline-flex w-full items-center justify-center rounded-full border border-accent/60 bg-accent px-5 py-2 text-sm font-medium text-paper transition hover:-translate-y-0.5 hover:bg-coral sm:w-auto"
        >
          Subscribe
        </button>
      </form>
    </section>
  </div>
</BaseLayout>
